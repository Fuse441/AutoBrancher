{
  "name": "autogenerate",
  "displayName": "autoGenerate",
  "description": "",
  "version": "1.0.0",
  "engines": {
    "vscode": "^1.99.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "autobrancher.helloWorld",
        "title": "Hello World"
      },
      {
        "command": "autobrancher.run",
        "title": "AutoBrancher: Run Branch Builder"
      },
      {
        "command": "generateScript.run",
        "title": "generateScript: Run Script Builder"
      }
    ]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@types/vscode": "^1.99.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1",
    "eslint": "^9.16.0"
  },
  "dependencies": {
    "isomorphic-git": "^1.30.1"
  }
}
